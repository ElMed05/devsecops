---
- name: Prepare Kubernetes node identity
  hosts: kubernetes
  become: true
  gather_facts: true

  roles:
    - role: node_identity
      tags: [identity]
    - role: kubernetes_prerequisites
      tags: [prerequisites]
    - role: container_runtime
      tags: [runtime]
    - role: kubernetes_packages
      tags: [kubernetes-packages]
