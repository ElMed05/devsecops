---
- name: Install the Kubernetes Gateway API and Istio
  hosts: control_plane
  become: true
  gather_facts: true
  roles:
    - role: argocd_bootstrap
      tags: [argocd]
    - role: gateway_api
      tags: [gateway-api]
    - role: istio_gateway
      tags: [istio, gateway]
